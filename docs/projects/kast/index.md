# Kast

Kast is a lightweight templating tool to process YAML files. It uses the Go template engine. It was born out of the idea of having a simple way to process Kubernetes manifests in Gitlab CI pipelines.
